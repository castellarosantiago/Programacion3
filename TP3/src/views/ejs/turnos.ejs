<!DOCTYPE html>
<html lang="es">
<head>
    <title>Turnos</title>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1>Turnos disponibles</h1>
    <ul>
        <% turnos.forEach(function(turno) { %>
            <li>
                Paciente: <%= turno.idPaciente %>, Fecha: <%= turno.fecha %>, Hora: <%= turno.hora %>
            </li>
        <% }); %>
    </ul>
    <footer>
        <p>© <%= new Date().getFullYear() %> ClinicalTurnos</p>
    </footer>
</body>
</html>